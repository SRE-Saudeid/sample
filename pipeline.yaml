
resources:
  repositories:
    - repository: templates
      type: git
      name: Area-Engenharia/templates
      ref: refs/heads/dev
#  #   
stages:
- template: BuildTemplates/azure-pipelines.yaml@templates
  parameters:
      namespace: 'poc'
      app: 'sample'
      coveragetest: false